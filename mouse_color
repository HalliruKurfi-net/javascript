draw = function() {
    fill(0, 255, 68); // start color
    rect(0, 0, 400, 200);  // the button
if(mouseIsPressed){
    fill(235, 23, 23);
    rect(0, 0, 400, 200);
}
    // The button text
    fill(0, 0, 0);
    textSize(30);
    text("Press me!", 145, 115);
};
